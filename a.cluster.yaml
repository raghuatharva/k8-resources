apiversion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: first-cluster
  region: ap-south-1

managednodegroups:
    - name: expense
      instancetype: t2.micro
      desiredcapacity: 3
      spot: true
      